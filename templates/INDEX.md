---
kind: index
subdir: <SUBDIR_NAME>
acs_version: "1.0"
updated: <YYYY-MM-DD>
---

# <SUBDIR_NAME> index — <PROJECT_NAME>

(One bullet per file under this subdirectory; description copied verbatim from each file's frontmatter `description`. Initially empty — populated as the subdir is filled.)
